[Unit]
Description=Qutebrowser Floccus Sync Daemon
After=network-online.target

[Service]
Type=simple
ExecStart={{BIN_PATH}}
Restart=on-failure
RestartSec=60s
Environment="WEBDAV_USER=secadm"

[Install]
WantedBy=default.target
